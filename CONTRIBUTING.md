
# CONTRIBUTING

이 문서는 templates 레포지토리의 협업 규정을 안내합니다.
작업 전에 반드시 읽어주세요.

---

## 브랜치 규칙

### 기본 구조

```
main                          ← 항상 배포 가능한 상태 (직접 push 금지)
└── feature/템플릿명-작업내용
└── fix/템플릿명-수정내용
└── docs/작업내용
```

### 브랜치 네이밍

| 유형 | 형식 | 예시 |
|------|------|------|
| 새 기능/작업 | `feature/템플릿명-작업내용` | `feature/mobile-scroll-header` |
| 버그 수정 | `fix/템플릿명-수정내용` | `fix/mobile-scroll-image-layout` |
| 문서 수정 | `docs/작업내용` | `docs/readme-update` |

> 한글도 괜찮지만 영어 소문자 + 하이픈(-) 을 권장합니다.

---

## 커밋 메시지 규칙

```
타입: 작업 내용 요약
```

| 타입 | 설명 | 예시 |
|------|------|------|
| `feat` | 새 기능 추가 | `feat: 메인 배너 섹션 추가` |
| `fix` | 버그 수정 | `fix: 모바일 하단 여백 오류 수정` |
| `style` | 디자인/스타일 변경 | `style: 폰트 사이즈 조정` |
| `docs` | 문서 수정 | `docs: README 템플릿 목록 추가` |
| `refactor` | 코드 구조 개선 | `refactor: CSS 클래스 정리` |
| `chore` | 설정 변경 등 기타 | `chore: 폴더 구조 정리` |

---

## PR(Pull Request) 규칙

### PR 생성 시

1. `main`에서 브랜치를 만들고 작업합니다
2. 작업이 끝나면 PR을 생성합니다
3. 제목 형식: `[템플릿명] 작업 내용 요약`
   - 예) `[mobile-scroll] 메인 배너 섹션 추가`
4. 변경 사항을 간단히 설명합니다
5. 관련 이슈가 있으면 연결합니다 (`Closes #이슈번호`)

## PR 리뷰 규칙

- 최소 1명의 승인 후 merge합니다
- 리뷰 요청 후 **당일 또는 다음 날**을 목표로 리뷰합니다
- 수정 요청이 있으면 반영 후 다시 리뷰 요청합니다
- merge는 **경력자가 담당**합니다 (익숙해지면 작성자 직접 merge로 전환)

---

## 새 템플릿 추가할 때

1. `feature/템플릿명-init` 브랜치 생성
2. 템플릿 폴더 생성 및 작업
3. 폴더 안에 `README.md` 작성 (스펙, 사용법 포함)
4. PR 생성 → 리뷰 → `main` merge
5. 루트 `README.md`의 템플릿 목록 업데이트

---

## 이슈 활용

작업 전에 이슈를 먼저 등록하는 것을 권장합니다.

- **기능 추가** → `Feature` 라벨
- **버그 수정** → `Bug` 라벨
- **문서 작업** → `Docs` 라벨
